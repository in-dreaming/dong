# Dong 重要特性一览

## 渲染架构

### Scene Graph 模式

跳过 DOM/CSS/Yoga 全流程，HTML 编译为扁平场景节点，O(1) 属性更新 + AABB hit-test。适用于游戏 UI（所有元素 `position: absolute` + 显式像素坐标）。

- 启用：`<meta name="dong-render-mode" content="scene">`
- JS API：`dong.scene.find(name)`, `dong.scene.set(id, prop, val)`, `dong.scene.on(id, event, fn)`
- 实现：`src/dom/scene_compiler.cpp`, `src/render/scene_graph.cpp`, `src/script/js_scene_bindings.cpp`

| Case | 说明 |
|:-----|:-----|
| `test_scene_compiler.html` | Scene Compiler 基本编译 |
| `test_scene_graph.html` | Scene Graph 渲染 + JS 交互 |
| `game_ui2.html` | 完整游戏 UI (gamelikeui/) |

### 双模渲染 (Retained + Immediate)

DOM retained mode (结构化 UI) 与 overlay immediate mode (高频动态内容) 共享同一 GPU 管线。

- Immediate API：`dong.drawRect()`, `dong.drawCircle()`, `dong.renderText()`, `dong.clearOverlay()`
- 实现：`src/render/overlay_draw.hpp`, `src/script/js_text_layout_bindings.cpp`

| Case | 说明 |
|:-----|:-----|
| `test_dual_mode.html` | 静态 DOM 框架 + 动态 overlay (粒子 + 文字流 + 障碍物) |
| `test_dual_mode_domonly.html` | DOM-only 基线对照 |

### GPU 性能优化

- **Display List skip-if-clean**：DOM 未变时复用缓存的 display list，只追加 overlay/scene items
- **Present-only 快速路径**：静态帧不重新执行 GPU draw calls，仅 blit intermediate → swapchain（~15x 提升）
- 实现：`src/core/engine_view.cpp` (`commands_regenerated_this_tick_`), `backends/sdl/sdl_gpu_driver_execute.cpp` (present-only)

### GlobalShared (多 View 资源共享)

同进程多 View 共享 GlyphAtlas，23 屏幕场景节省 ~936MB GPU 内存。

- C API：`include/dong_global_shared.h`
- 实现：`src/core/global_shared.cpp`

| Case | 说明 |
|:-----|:-----|
| `3d_screens_simple/main.c` | 3D 场景中多个 HTML 屏幕共享资源 |

---

## CSS 布局特性

### position: sticky

布局时占位 + 渲染时视觉调整，支持嵌套 sticky、containing block 夹紧。

| Case | 说明 |
|:-----|:-----|
| `test_sticky_scroll_top.html` | 顶部吸附 |
| `test_sticky_scroll_bottom.html` | 底部吸附 |
| `test_sticky_parent_clamp.html` | 父容器夹紧 |
| `test_sticky_nested.html` | 嵌套 sticky |

### aspect-ratio

Pre-layout 维度解析 (Yoga 之前)，支持 min/max 约束、replaced elements、flex 约束模式。

| Case | 说明 |
|:-----|:-----|
| `test_aspect_ratio_width_auto_height.html` | width + auto height |
| `test_aspect_ratio_min_max.html` | min/max 约束交互 |
| `test_aspect_ratio_flex.html` | flex 容器中的 aspect-ratio |

### display: contents / flow-root

- `contents`：Yoga 树手术，子节点附着到祖父节点，伪元素正常渲染
- `flow-root`：BFC margin collapse 阻断

| Case | 说明 |
|:-----|:-----|
| `test_display_contents_layout.html` | contents 布局 |
| `test_display_contents_pseudo.html` | contents + 伪元素 |
| `test_display_contents_events.html` | contents 事件冒泡 |
| `test_flow_root_margin_collapse.html` | flow-root margin collapse |

---

## CSS 样式特性

### 列表样式 & ::marker

支持 disc/circle/square/decimal/alpha/roman，`list-style-position: outside|inside`，`::marker` 独立样式。

| Case | 说明 |
|:-----|:-----|
| `test_list_markers_basic.html` | 基本标记渲染 |
| `test_list_style_types.html` | 各类型标记 |
| `test_list_position.html` | outside vs inside |
| `test_marker_pseudo.html` | ::marker 伪元素样式 |
| `test_list_nested.html` | 嵌套列表计数器 |
| `test_ol_start.html` | `<ol start="N">` |

### CSS 其他

| Case | 说明 |
|:-----|:-----|
| `test_border_radius_percent.html` | 百分比圆角 |
| `test_border_collapse.html` | 表格边框合并 |
| `text_decoration_test.html` | 文本装饰线 |
| `text_shadow_test.html` | 文本阴影 |
| `transform_test.html` | CSS transform |
| `test_opacity_percent.html` | 百分比透明度 |
| `test_overflow_clip.html` | overflow: clip |
| `test_css_layer_basic.html` | @layer 层叠层 |
| `test_css_supports.html` | @supports 查询 |
| `test_logical_properties.html` | 逻辑属性 (inline/block) |
| `test_oklab_color.html` | oklab() 颜色 |
| `test_oklch_color.html` | oklch() 颜色 |
| `test_color_mix.html` | color-mix() |
| `test_light_dark_function.html` | light-dark() |
| `test_env_function.html` | env() 函数 |
| `test_css_hyphens_soft_hyphen.html` | 连字符断词 |
| `test_css_image_rendering.html` | image-rendering |
| `test_css_resize_textarea.html` | resize 属性 |

---

## 表单元素

### `<select>` 下拉列表

点击展开/收起、选项选择、键盘导航 (Arrow/Enter/Escape)、`change` 事件、JS API (`selectedIndex`, `value`, `options`)。

| Case | 说明 |
|:-----|:-----|
| `test_select_basic.html` | 基本渲染 |
| `test_select_keyboard.html` | 键盘导航 |
| `test_select_dropdown_overlay.html` | 下拉覆盖层 |
| `test_select_option_click.html` | 选项点击 |
| `test_select_programmatic_setters.html` | JS 设置 |

### `<input>` / `<textarea>`

光标定位、文本选择、Backspace/Delete/Arrow 编辑、maxlength 验证、placeholder、readonly/disabled。

| Case | 说明 |
|:-----|:-----|
| `test_input_caret.html` | 光标渲染 |
| `test_input_maxlength.html` | 最大长度 |
| `test_input_readonly.html` | 只读状态 |
| `test_textarea_multiline.html` | 多行编辑 |
| `test_textarea_rows_cols.html` | rows/cols 尺寸 |

### 其他表单

| Case | 说明 |
|:-----|:-----|
| `test_checkbox_toggle.html` | 复选框切换 |
| `test_label_for.html` | label for 关联 |
| `test_form_validation_comprehensive.html` | 表单验证 |
| `test_disabled.html` | disabled 状态 |

---

## DOM / JavaScript

### ContentEditable

| Case | 说明 |
|:-----|:-----|
| `test_contenteditable_basic.html` | 基本编辑 |
| `test_contenteditable_typing.html` | 输入测试 |
| `test_contenteditable_bold_auto.html` | 粗体切换 |
| `test_contenteditable_features.html` | 综合功能 |

### DOM API

| Case | 说明 |
|:-----|:-----|
| `test_dom_manipulation.html` | DOM 增删改 |
| `test_dom_element_interface.html` | Element 接口 |
| `test_dom_node_interface.html` | Node 接口 |
| `test_document_methods.html` | Document 方法 |
| `test_mutation_observer.html` | MutationObserver |
| `test_selection_api.html` | Selection API |

### 事件系统

| Case | 说明 |
|:-----|:-----|
| `test_event_dispatch_semantics.html` | 事件派发语义 |
| `test_event_currenttarget.html` | currentTarget |
| `test_event_features.html` | 综合事件特性 |
| `test_events_keyboard.html` | 键盘事件 |
| `test_events_scroll.html` | 滚动事件 |
| `test_drag_drop_basic.html` | 拖放 |

### 其他 JS API

| Case | 说明 |
|:-----|:-----|
| `test_fetch_json_basic.html` | Fetch API (JSON) |
| `test_cssom_methods.html` | CSSOM 方法 |
| `test_cssom_matchmedia.html` | matchMedia |
| `test_performance_now.html` | performance.now() |
| `test_domparser_api.html` | DOMParser |
| `test_formdata_api.html` | FormData |

---

## HTML 元素

| Case | 说明 |
|:-----|:-----|
| `test_details_element.html` | `<details>/<summary>` 折叠 |
| `test_dialog_modal.html` | `<dialog>` 模态 |
| `test_dialog_backdrop.html` | `::backdrop` |
| `test_hidden_attr.html` | hidden 属性 |
| `test_inert_attribute.html` | inert 属性 |
| `test_optgroup_basic.html` | `<optgroup>` |
| `test_img_load_error.html` | 图片加载事件 |
| `test_title_tooltip.html` | title 工具提示 |

---

## 滚动

| Case | 说明 |
|:-----|:-----|
| `test_scroll_behavior_smooth.html` | smooth 滚动 |
| `test_scroll_properties.html` | scrollTop/scrollLeft |
| `test_overscroll_behavior_none.html` | overscroll-behavior |
| `test_scroll_container_mid_scroll.html` | 中间位置滚动 |

---

## 文本渲染

| Case | 说明 |
|:-----|:-----|
| `test_text_flow.html` | 障碍物绕排文字 (静态) |
| `test_text_flow_dynamic.html` | 障碍物绕排文字 (动态) |
| `test_text_flow_directdraw.html` | Direct Draw 文字流 |
| `chinese_font_large_test.html` | 中文大字号 |
| `test_font_weight.html` | 字重匹配 |
| `test_font_weight_100_900.html` | 100-900 字重 |
| `font_style_test.html` | 字体样式 (italic/oblique) |
| `test_whitespace_prewrap.html` | white-space: pre-wrap |
| `test_break_spaces_key_features.html` | break-spaces |

---

## 运行方式

```bash
cd dong/zig-out/bin

# Scene Graph 游戏 UI
dong_app.exe --html data\gamelikeui\game_ui2.html

# 双模渲染
dong_app.exe --html data\tests\test_dual_mode.html

# 任意测试用例
dong_app.exe --html data\tests\<case>.html

# headless 渲染
html_render_test.exe <html_file> [output.bmp] [width] [height]

# 批量渲染所有测试
zig build render-all-tests
```
