
重要：
- 核心思路是梳理重构整个架构，不留技术债。dong引擎要持有访问的东西如果在backend，就做成plugin注入

# Dong 引擎重构遗留清单

> 最后更新: 2026-02-09（P0+P1+2.2+2.5+2.7完成，测试验证通过）
> 用途: 跟踪重构进度，避免重复搜集

---

## 零、已完成大项（已归档）

<details>
<summary>✅ 点击查看已完成大项</summary>

### SDL Log 迁移到 DongLogger - 已完成
全部 SDL_Log 已替换为 DONG_LOG_* 宏。

### GPU 压缩纹理支持 - 已完成（2026-02-07）
- ASTC 4x4/5x5/6x6/8x8 编码（Compute Shader）
- BC7/BC6H 编码（CPU + GPU）
- ImageAtlas 自动压缩（GPU直写Atlas）
- 环境变量 `DONG_ATLAS_FORMAT` 控制
- 文件：`gpu_texture_compressor.hpp/cpp`, `shaders/cmp/*`

### 方案B阶段1-4 - 已完成
- Backend Plugin 化
- GPUDriverSDL 迁移到 backends/sdl
- sdl_render/* 迁移
- 着色器资源管理

### GlobalShared 资源共享 - 已完成（2026-02-06）
- 同进程多 View 共享 GlyphAtlas
- 23 屏幕场景节省 ~936MB GPU 内存（从 ~1GB 降到 ~64MB）
- C API: `dong_global_shared.h`
- 文件：`src/core/global_shared.hpp/cpp`, `src/core/global_shared_c_api.cpp`

### View/Engine 统一 - 已完成（2026-02-07）
- 删除旧 `src/core/view.cpp/hpp`（1800+ 行 SDL 强耦合代码）
- 统一使用 `src/core/engine_view.cpp`（基于 `DongGPUDriver` C 接口）
- 废弃 C++ `GPUDriver` 抽象，Core 层只保留 C 接口

### Core 层 SDL 依赖清理 - 已完成（2026-02-06）
扫描确认以下文件**无实际 SDL 依赖**：
- `src/core/view.cpp` - 无 SDL 头文件，使用 DongGPUDriver C 接口
- `src/core/view.hpp` - 无 SDL 前向声明
- `src/render/display_list.hpp` - 仅注释含 SDL_Log，无实际代码依赖
- `src/render/gpu_driver.hpp` - 纯抽象接口，仅枚举值 `SDL_GPU=0`，无 SDL 头文件
- `src/render/glyph_atlas.hpp` - 无 SDL include，仅注释提及 SDL_GPUTexture
- `include/*.h` - 无 SDL include，仅注释提及 SDL 类型作为 API 文档

**迁移完成**：以下文件已移到 `backends/sdl/`
- `gpu_device` → `sdl_gpu_device`
- `gpu_painter` → `sdl_gpu_painter`
- `gpu_surface` → `sdl_gpu_surface`
- `shader_manager` → `sdl_shader_manager`

### 功能 TODO 已完成项 - 已归档（2026-02-06）

| 项 | 说明 |
|---|------|
| `view.cpp` WHEEL 事件 | `handle_mouse_wheel()` 已实现，含 delta_x/delta_y 处理 |
| `view.cpp` delta 存储 | `dispatchWheelEventToJS()` 已实现 |
| `script_engine.cpp` console.log | `js_console_log()` 已实现并注册到 global |
| `resource_manager.cpp` loadImage | 已通过 Platform 注入 IImageDecoder 实现 |
| `resource_manager.cpp` loadImageFromMemory | 已通过 Platform 注入 IImageDecoder 解码 |
| `dom_node.cpp` DOM 方法 | `scrollIntoView()`, `focus()`, `blur()`, `click()` 已实现 |
| IImageDecoder 接口 | 定义接口 + SDL 实现 + GPU 压缩支持 |

</details>

---

## 一、SDL 依赖泄漏（Core 层不应有 SDL）（已完成）


## 二、功能 TODO 列表

### 2.1 ResourceManager（已完成）

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `resource_manager.cpp:24` | 使用 FreeType + GlyphAtlas 替代字体管理 | ✅ | FontResource 使用 FT_Face |
| `resource_manager.cpp:31` | ImageResource 析构 | ✅ | 像素数据由 atlas 管理 |
| `resource_manager.cpp:36` | FontResource 析构 | ✅ | 释放 FT_Face |
| `resource_manager.cpp:231-232` | loadFont: 使用 FreeType 加载字体 | ✅ | 使用 font_resolver + FreeType |
| `resource_manager.cpp:247-248` | getSystemFont: 使用 FreeType + 系统字体查找 | ✅ | 使用 resolveFontPath |

### 2.2 RenderSurface（已完成）

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `render_surface.cpp:53` | GPUTextureSurface 析构 | ✅ | 由 SDL 后端 `~GPUTextureSurfaceImpl()` 实现 |
| `render_surface.cpp:57` | clear() GPU 清空 | ✅ | 由后端在渲染 pass 中处理 |
| `render_surface.cpp:62` | lock() GPU->CPU 同步 | ✅ | SDL 后端自动处理 |
| `render_surface.cpp:67` | unlock() CPU->GPU 同步 | ✅ | SDL 后端自动处理 |

### 2.3 GPUPainter

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `gpu_painter.cpp:73-74` | renderDisplayListInternal: stub | ❌ | 实际渲染未实现 |
| `gpu_painter.cpp:115` | 设置渲染管线 | ❌ | |

### 2.4 FontFinder（系统字体查找）

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `font_finder.cpp:189` | Windows DirectWrite 实现 | ⚠️ | 基础框架存在，完整实现需 COM |
| `font_finder.cpp:196` | Linux Fontconfig 实现 | ⚠️ | 基础框架存在，完整实现需 libfontconfig |
| `font_resolver.cpp:479` | Windows/Linux system lookup | ✅ | 硬编码路径候选方案工作正常 |

### 2.5 DOM/CSS ✅ 已完成

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `dom_node.cpp:576` | innerHTML setter: Parse HTML and add children | ✅ | 已实现 |
| `dom_node.cpp:627` | outerHTML setter: Parse HTML and replace self | ✅ | 已实现 |
| `dom_node.cpp:649` | insertAdjacentHTML: Parse HTML | ✅ | 已实现 |
| `html_parser.cpp:413` | Load external stylesheet | ❌ | 低优先级，暂不实现 |
| `selector_matcher.cpp:491` | :focus-visible 伪类 | ✅ | 已实现 |
| `focus_manager.cpp:113` | 完整的 tabindex 排序 | ✅ | 已实现 |

**测试验证**: 
- `innerhtml_test.html` - innerHTML/outerHTML/insertAdjacentHTML 功能正常
- `focus_visible_test.html` - :focus-visible/:focus-within 伪类匹配正常
- `tabindex_test.html` - tabindex 排序和导航顺序正常

### 2.6 View

（本节所有任务已完成并归档）

### 2.7 Script Engine ✅ 已完成

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `script_engine.cpp:332` | bindGlobalFunction 实现 | ✅ | 已实现 |
| `script_engine.cpp:338` | Promise/微任务队列 | ✅ | 已实现 |

**实现详情**:
- `bindGlobalFunction()`: 使用 `JS_NewCFunction()` 将 C 函数绑定到 JS 全局对象
- `processPendingTasks()`: 使用 `JS_ExecutePendingJob()` 处理 Promise 微任务队列

**测试验证**: 
- 所有测试 HTML 中的 JavaScript 代码正常执行
- 内联事件处理器（onclick）正确绑定和触发
- `addEventListener` 事件监听功能正常

### 2.8 其他

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `context.cpp:22` | 初始化平台特定资源 | ❌ | 可能不需要 |
| `observer.cpp:313` | IntersectionObserver 使用实际时间戳 | ❌ | |
| `style_engine.hpp:2` | 更新 include 路径 | ❌ | 兼容头 |
| `parser.hpp:2` | 更新 include 路径 | ❌ | 兼容头 |

### 2.9 AppCore

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `overlay.c:436` | hit_test: 检查像素透明度 | ❌ | 返回 0 |

---

## 三、WORKAROUND 列表

| 位置 | 问题 | 状态 | 说明 |
|------|------|------|------|
| `layout_engine.cpp:956-1014` | Yoga 计算超大高度值 (>100000) | ⚠️ 仍存在 | 已检测并修正（行 988-1014），需根治 Yoga bug |
| `sdl_gpu_driver_execute.cpp:810` | 隔离层切换时拆分 command buffer | ⚠️ 仍存在 | `split_cmd_buf_for_isolated_layers` 配置规避多 pass 内容丢失 |

---

## 四、接口注入重构（按优先级）

### P1 - 短期需要（已完成）

| 接口 | 当前状态 | 说明 |
|------|----------|------|
| IGlyphAtlas | ✅ 已完成 | `dong_glyph_atlas.h` C 接口 |
| IGPUDevice | ✅ 已完成 | 通过 `DongGPUDriver` C 接口 |
| DongFileSystem | ✅ 已完成 | Core 全部使用 `dong_fs_*` |
| DongLogger | ✅ 已完成 | 全部使用 `DONG_LOG_*` |

### P2 - 中期需要

| 接口 | 当前状态 | 目标 |
|------|----------|------|
| IFontLoader | 不存在 | 字体加载抽象 |
| IShaderManager | SDL 硬编码 | 着色器管理抽象 |

---

## 五、完成记录

| 日期 | 完成项 | 提交 |
|------|--------|------|
| 2026-02-03 | 定义 `dong_image_decoder.h` 接口 | - |
| 2026-02-03 | 实现 SDL ImageDecoder (stb_image 解码) | - |
| 2026-02-03 | 更新 `dong_platform.h` 添加 ImageDecoder 注入 | - |
| 2026-02-03 | 创建 `tests/test_image_decoder.c` 单测 | - |
| 2026-02-03 | 实现 ASTC 编码器 (`dong_astc_encoder.c`) | - |
| 2026-02-03 | 实现 BC7/BC6H 编码器 (`dong_bc_encoder.c`) | - |
| 2026-02-03 | 集成编码器到 SDL ImageDecoder | - |
| 2026-02-03 | 重构 `resource_manager.cpp` 移除 SDL 依赖 | - |
| 2026-02-03 | 定义 `dong_image_atlas.h` 接口（block-aligned 分配） | - |
| 2026-02-03 | 实现 SDL ImageAtlas (`dong_sdl_image_atlas.c`) | - |
| 2026-02-03 | 添加 ImageAtlas 单元测试 | - |
| 2026-02-03 | 渲染器集成 ImageAtlas（替换旧的 image_atlas_texture_） | - |
| 2026-02-03 | 添加压缩格式 Atlas 支持（GPU 检测 + 自动编码） | - |
| 2026-02-03 | glyph_atlas.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | gpu_device.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | gpu_painter.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | gpu_surface.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | shader_manager.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | view.cpp 移除 SDL_Log (47处)，使用 DongLogger | - |
| 2026-02-04 | gpu_driver_sdl.cpp 移除 SDL_Log (12处) | - |
| 2026-02-04 | gpu_driver_sdl_init.cpp 移除 SDL_Log (23处) | - |
| 2026-02-04 | gpu_driver_sdl_execute.cpp 移除 SDL_Log (43处) | - |
| 2026-02-04 | gpu_driver_sdl_resources.cpp 移除 SDL_Log (9处) | - |
| 2026-02-04 | gpu_driver_sdl_fonts.cpp 移除 SDL_Log (1处) | - |
| 2026-02-04 | sdl3_window.cpp 移除 SDL_Log (11处) | - |
| 2026-02-04 | 架构重构: src/platform/sdl3_window.* → backends/sdl/sdl_window.* | - |
| 2026-02-04 | 架构重构: src/input/input_adapter_sdl3.* → backends/sdl/sdl_input_adapter.* | - |
| 2026-02-04 | 更新 CMakeLists.txt 移除 dong_render 中的 SDL 平台文件 | - |
| 2026-02-04 | 阶段1: 创建 backends/sdl/dong_sdl_plugin.h/cpp (Plugin 注入) | - |
| 2026-02-04 | 阶段1: 实现 DongPlugin vtable 注入 DongGPUDriver 到 Platform | - |
| 2026-02-06 | 阶段2-4: 迁移 GPUDriverSDL 到 backends/sdl | - |
| 2026-02-06 | 3D场景性能优化: 0.3 FPS → 300-400 FPS | - |
| 2026-02-06 | 视频帧上传修复：支持active frame时长时间command buffer | - |
| 2026-02-06 | GlobalShared资源共享：23屏幕节省~936MB GPU内存 | - |
| 2026-02-07 | GPU纹理压缩框架：`gpu_texture_compressor.hpp/cpp` | - |
| 2026-02-07 | BC7 Compute Shader：`shaders/cmp/bc7_encode_cs.hlsl` | - |
| 2026-02-07 | ASTC Compute Shader：`shaders/cmp/astc_encode_cs.hlsl` | - |
| 2026-02-07 | ShaderManager 添加 compute pipeline 支持 | - |
| 2026-02-07 | SDLGPUDriver 集成 GPU 压缩器 | - |
| 2026-02-07 | GPU压缩直写Atlas：`compressToTextureRegion()` | - |
|| 2026-02-08 | DOM: innerHTML/outerHTML setter implemented | - |
|| 2026-02-08 | DOM: insertAdjacentHTML implemented | - |
|| 2026-02-08 | CSS: :focus-visible pseudo-class implemented | - |
|| 2026-02-08 | FocusManager: full tabindex sorting implemented | - |
|| 2026-02-08 | ScriptEngine: bindGlobalFunction implemented | - |
|| 2026-02-08 | ScriptEngine: Promise/microtask queue implemented | - |
|| 2026-02-08 | JSBindings: innerHTML getter/setter for JS element API | - |
|| 2026-02-09 | 2.5 DOM/CSS 全部功能完成并测试验证 | - |
|| 2026-02-09 | 2.7 Script Engine 全部功能完成并测试验证 | - |
|| 2026-02-09 | 创建测试 HTML: innerhtml_test.html | - |
|| 2026-02-09 | 创建测试 HTML: focus_visible_test.html | - |
|| 2026-02-09 | 创建测试 HTML: tabindex_test.html | - |

---

## 六、View/Engine 统一 - 已完成（2026-02-07）

**已完成**:
- 删除旧 `src/core/view.cpp` 和 `src/core/view.hpp`（1800+ 行 SDL 强耦合代码）
- 统一使用 `src/core/engine_view.cpp`（基于 `DongGPUDriver` C 接口）
- `api_bindings.cpp` 已使用 `EngineView` 实现 C API

---

## 七、方案B：彻底架构重构蓝图

> 决策日期: 2026-02-04  
> 核心原则: 梳理重构整个架构，不留技术债。dong引擎要持有访问的东西如果在backend，就做成plugin注入。

### 7.1 目标架构（方案B）

见 doc\arch\arch.md

### 7.2 关键变化（方案B vs 当前）

| 方面 | 当前状态 | 方案B目标 |
|------|----------|-----------|
| **GPUDriver 位置** | `src/render/gpu_driver_sdl.*` (core src) | `backends/sdl/sdl_gpu_driver.*` (backend) ✅ 已完成 |
| **抽象层** | C++ `class GPUDriver` (core 保留) | 只用 C `DongGPUDriver` (backend 提供) ✅ 已完成 |
| **着色器管理** | `src/render/shader_manager.*` (core) | `backends/sdl/sdl_shader_manager.*` (backend) ✅ 已完成 |
| **字图集** | `src/render/glyph_atlas.*` (core, 含 SDL) | `backends/sdl/sdl_glyph_atlas.*` (backend) ✅ GlobalShared已解耦 |
| **GPUDevice** | `src/render/gpu_device.*` (core, 含 SDL) | `backends/sdl/sdl_gpu_device.*` ✅ 已完成 |
| **sdl_render/** | `src/render/sdl_render/*` | `backends/sdl/` ✅ 已完成 |
| **着色器资源** | `src/render/sdl_render/shaders/` | `backends/sdl/shaders/` ✅ 已完成 |
| **View/Engine** | 双维护 `view.cpp` + `engine_view.cpp` | 统一 `engine_view.cpp` ✅ 已完成 |

### 7.3 实施阶段

#### 阶段1: Backend Plugin 化 ✅ 已完成

**目标**: Backend 通过 `DongPlugin` 接口注入 `DongGPUDriver`

**新增文件**:
- `backends/sdl/dong_sdl_plugin.h` - SDL backend 的 plugin 入口 ✅
- `backends/sdl/dong_sdl_plugin.cpp` - 实现 plugin vtable ✅

#### 阶段2: 迁移 GPUDriverSDL ✅ 已完成

**目标**: 将 `GPUDriverSDL` 从 core 移到 backend

**步骤**:
1. ✅ 创建 `backends/sdl/sdl_gpu_driver.hpp/cpp` 框架 (Phase 2A)
2. ✅ 创建 `backends/sdl/sdl_gpu_driver_init.cpp` - 初始化/管线创建
3. ✅ 创建 `backends/sdl/sdl_gpu_driver_execute.cpp` - 命令执行/渲染
4. ✅ 创建 `backends/sdl/sdl_gpu_driver_resources.cpp` - 资源管理/图集
5. ✅ 创建 `backends/sdl/sdl_gpu_driver_fonts.cpp` - 字体/Glyph管理
6. ✅ 更新 `backends/sdl/CMakeLists.txt` 添加新文件

#### 阶段3: 迁移 sdl_render/* ✅ 已完成（合并到阶段2）

#### 阶段4: 着色器资源管理 ✅ 已完成

**目标**: 着色器资源放到 backend，并统一管理

**新目录结构**:
```
backends/sdl/
├── shaders/
│   ├── rect.hlsl          # 纯色矩形
│   ├── round_rect.hlsl    # 圆角矩形
│   ├── shadow.hlsl        # 阴影
│   ├── image.hlsl         # 图片
│   ├── text.hlsl          # 文字(MSDF)
│   ├── video_rgba.hlsl    # 视频 RGBA
│   └── video_yuv.hlsl     # 视频 YUV420P
```


## 八、后续优化方向

### 8.1 性能优化

| 优化点 | 优先级 | 说明 |
|--------|--------|------|
| CommandList 池化 | P1 | 减少每帧内存分配 |
| GlyphAtlas 动态扩容 | P2 | 当前固定 2048x2048 |
| 批量渲染合并 | P1 | 减少 draw call |
| GPU 压缩纹理默认启用 | P2 | 当前需环境变量开启 |

### 8.2 资源共享扩展

| 资源 | 当前状态 | 优化后 |
|------|----------|--------|
| GlyphAtlas | ✅ GlobalShared 共享 | 已完成 |
| ImageAtlas | 每 View 独立 | 可共享（内容相同） |
| Shader/Pipeline | 每 View 独立 | 可共享 |
| FontResolver | 每 View 独立 | 可共享 |

### 8.3 渲染质量

| 功能 | 状态 | 说明 |
|------|------|------|
| MSDF 字体 | ✅ 已实现 | 多 tier 支持 |
| 子像素渲染 | ⚠️ 部分 | LCD 过滤未启用 |
| Gamma 校正 | ⚠️ 部分 | 需要 sRGB 工作流 |
| HDR 支持 | ❌ 未开始 | 需要 DXGI swapchain |
|| 2026-02-07 | P0��ɣ�ɾ����View��ͳһʹ��EngineView | - |
|| 2026-02-07 | P1��ɣ�ResourceManager�������ʵ�� | - |
|| 2026-02-07 | P1��ɣ�IGlyphAtlas����ӿ� | - |
|| 2026-02-07 | P1��ɣ�DongFileSystem/DongLoggerͳһʹ�� | - |
|| 2026-02-07 | 2.2��ɣ�RenderSurface GPU��Դ�����ɺ��ʵ�� | - |
