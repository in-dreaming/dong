# Dong 引擎重构遗留清单

> 最后更新: 2026-02-03
> 用途: 跟踪重构进度，避免重复搜集

---

## 一、SDL 依赖泄漏（Core 层不应有 SDL）

### 1.1 严重 - 必须重构

| 文件 | 问题 | 状态 | 重构方案 |
|------|------|------|----------|
| `src/core/view.cpp` | `#include <SDL3/SDL_log.h/SDL_gpu.h/SDL_filesystem.h>`, SDL 类型遍布 | ❌ 未开始 | 移到 backends/sdl 或抽象为接口 |
| `src/core/view.hpp` | 前向声明 `SDL_GPUDevice/SDL_GPUTexture/SDL_Window` | ❌ 未开始 | 使用平台无关类型 |
| `src/render/resource_manager.cpp` | 已重构，使用 Platform 注入的 FileSystem 和 ImageDecoder | ✅ 完成 | IImageDecoder + IFileSystem |
| `src/render/glyph_atlas.hpp:8` | `#include <SDL3/SDL_gpu.h>`, `SDL_GPUTexture*` | ❌ 未开始 | 抽象 IGlyphAtlas 接口 |
| `src/render/glyph_atlas.cpp` | 移除 SDL_Log，使用 DongLogger | ✅ 完成 | DONG_LOG_* 宏 |
| `src/render/gpu_device.hpp:6` | `#include <SDL3/SDL_gpu.h>` | ❌ 未开始 | 抽象 IGPUDevice 接口 |
| `src/render/gpu_device.cpp` | 移除 SDL_Log，使用 DongLogger | ✅ 完成 | DONG_LOG_* 宏 |
| `src/render/gpu_driver.hpp:7` | `#include <SDL3/SDL_gpu.h>` | ❌ 未开始 | 分离 abstract/sdl 实现 |
| `src/render/gpu_painter.hpp:4` | `#include <SDL3/SDL_gpu.h>` | ❌ 未开始 | 移除 SDL 依赖 |
| `src/render/gpu_painter.cpp` | 移除 SDL_Log，使用 DongLogger | ✅ 完成 | DONG_LOG_* 宏 |
| `src/render/gpu_surface.hpp:4` | `#include <SDL3/SDL_gpu.h>` | ❌ 未开始 | 抽象接口 |
| `src/render/gpu_surface.cpp` | 移除 SDL_Log，使用 DongLogger | ✅ 完成 | DONG_LOG_* 宏 |
| `src/render/shader_manager.hpp:7` | `#include <SDL3/SDL_gpu.h>` | ❌ 未开始 | 移到 SDL backend |
| `src/render/shader_manager.cpp:3-4` | `#include <SDL3/SDL_log.h>`, `SDL_shadercross` | ❌ 未开始 | 移到 SDL backend |

### 1.2 架构问题 - 文件位置错误

| 文件 | 问题 | 状态 | 重构方案 |
|------|------|------|----------|
| `src/platform/sdl3_window.*` | SDL 实现放在 core src 下 | ❌ 未开始 | 移到 `backends/sdl/` |
| `src/input/input_adapter_sdl3.*` | SDL 实现放在 core src 下 | ❌ 未开始 | 移到 `backends/sdl/` |
| `src/render/gpu_driver_sdl.*` | SDL 实现放在 core src 下 | ❌ 未开始 | 移到 `backends/sdl/` |
| `src/render/sdl_render/*` | 整个目录应在 backends | ❌ 未开始 | 移到 `backends/sdl/` |

### 1.3 可接受 - Legacy 或已隔离

| 文件 | 说明 |
|------|------|
| `src/api_bindings_legacy.cpp` | Legacy API，SDL 耦合是预期的 |

---

## 二、功能 TODO 列表

### 2.1 ResourceManager（高优先级）

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `resource_manager.cpp:24` | 使用 FreeType + GlyphAtlas 替代字体管理 | ❌ | 当前字体管理分散 |
| `resource_manager.cpp:31` | ImageResource 析构：清理图片资源 | ❌ | 需要正确释放 |
| `resource_manager.cpp:36` | FontResource 析构：清理字体资源 | ❌ | 需要正确释放 |
| `resource_manager.cpp` | loadImage: 使用 IImageDecoder 加载图片 | ✅ | 已通过 Platform 注入 |
| `resource_manager.cpp` | loadImageFromMemory: 使用 IImageDecoder 解码 | ✅ | 已通过 Platform 注入 |
| `resource_manager.cpp:231-232` | loadFont: 使用 FreeType 加载字体 | ❌ | 需要 IFontLoader |
| `resource_manager.cpp:247-248` | getSystemFont: 使用 FreeType + 系统字体查找 | ❌ | 需要 FontFinder |

### 2.2 RenderSurface

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `render_surface.cpp:53` | GPUTextureSurface 析构：释放 GPU 纹理 | ❌ | 注释提到 OpenGL/Vulkan |
| `render_surface.cpp:57` | clear(): 通过 OpenGL/Vulkan API 清空 | ❌ | 混淆的后端引用 |
| `render_surface.cpp:62` | lock(): GPU->CPU 同步 | ❌ | |
| `render_surface.cpp:67` | unlock(): CPU->GPU 同步 | ❌ | |

### 2.3 GPUPainter

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `gpu_painter.cpp:73-74` | renderDisplayListInternal: stub | ❌ | 实际渲染未实现 |
| `gpu_painter.cpp:115` | 设置渲染管线 | ❌ | |

### 2.4 FontFinder（系统字体查找）

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `font_finder.cpp:189` | Windows DirectWrite 实现 | ❌ | 仅 stub |
| `font_finder.cpp:196` | Linux Fontconfig 实现 | ❌ | 仅 stub |
| `font_finder.cpp:253` | 初始化 DirectWrite | ❌ | |
| `font_finder.cpp:258` | 初始化 Fontconfig | ❌ | |
| `font_finder.cpp:275` | 清理 DirectWrite | ❌ | |
| `font_finder.cpp:278` | 清理 Fontconfig | ❌ | |
| `font_resolver.cpp:479` | Windows/Linux system lookup 是 stub | ❌ | 主要靠路径候选 |

### 2.5 DOM/CSS

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `dom_node.cpp:573` | innerHTML setter: Parse HTML and add children | ❌ | |
| `dom_node.cpp:617` | outerHTML setter: Parse HTML and replace self | ❌ | |
| `dom_node.cpp:628` | insertAdjacentHTML: Parse HTML | ❌ | |
| `dom_node.cpp:841` | scrollIntoView: Implement | ❌ | |
| `dom_node.cpp:1007` | focus(): Implement focus management | ❌ | |
| `dom_node.cpp:1011` | blur(): Implement focus management | ❌ | |
| `dom_node.cpp:1015` | click(): Dispatch click event | ❌ | |
| `html_parser.cpp:413` | Load external stylesheet | ❌ | |
| `selector_matcher.cpp:491` | :focus-visible 伪类 | ❌ | 需要 focus-ring 启发式 |
| `focus_manager.cpp:113` | 完整的 tabindex 排序 | ❌ | |

### 2.6 View

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `view.cpp:1727` | 添加 WHEEL 事件类型 | ❌ | |
| `view.cpp:1732` | 将 delta_x/delta_y 存入事件数据 | ❌ | |

### 2.7 Script Engine

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `script_engine.cpp:332` | bindGlobalFunction 实现 | ❌ | 编码损坏 |
| `script_engine.cpp:338` | Promise/微任务队列 | ❌ | 编码损坏 |
| `script_engine.cpp:346` | console.log 实现 | ❌ | 编码损坏 |

### 2.8 其他

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `context.cpp:22` | 初始化平台特定资源 | ❌ | 可能不需要 |
| `observer.cpp:313` | IntersectionObserver 使用实际时间戳 | ❌ | |
| `style_engine.hpp:2` | 更新 include 路径 | ❌ | 兼容头 |
| `parser.hpp:2` | 更新 include 路径 | ❌ | 兼容头 |

### 2.9 AppCore

| 位置 | TODO | 状态 | 说明 |
|------|------|------|------|
| `overlay.c:436` | hit_test: 检查像素透明度 | ❌ | 返回 0 |

---

## 三、WORKAROUND 列表

| 位置 | 问题 | 状态 | 说明 |
|------|------|------|------|
| `layout_engine.cpp:956,982` | Yoga 计算超大高度值 (>100000) | ⚠️ | 已有 workaround，需要根治 |
| `gpu_driver_sdl.hpp:73,88` | 隔离层切换时拆分 command buffer | ⚠️ | 规避多 pass 内容丢失 |

---

## 四、接口注入重构（按优先级）

### P0 - 立即需要

| 接口 | 当前状态 | 目标 |
|------|----------|------|
| **IImageDecoder** | ✅ 已完成 | 定义接口 + SDL 实现 + GPU 压缩支持 |

### P1 - 短期需要

| 接口 | 当前状态 | 目标 |
|------|----------|------|
| IGlyphAtlas | SDL 硬编码 | 抽象接口 |
| IGPUDevice | SDL 硬编码 | 抽象接口 |
| DongFileSystem | 接口存在，未统一使用 | Core 全部走注入 |
| DongLogger | 接口存在，未统一使用 | 替换所有 SDL_Log |

### P2 - 中期需要

| 接口 | 当前状态 | 目标 |
|------|----------|------|
| IFontLoader | 不存在 | 字体加载抽象 |
| IShaderManager | SDL 硬编码 | 着色器管理抽象 |

---

## 五、GPU 压缩纹理支持（新功能）

### 5.1 目标格式

| 格式 | 用途 | 平台支持 |
|------|------|----------|
| ASTC 4x4 | 通用压缩 | 移动端 + 现代桌面 |
| BC6H | HDR 压缩 | 桌面 (D3D11+/Vulkan) |
| BC7 | 高质量压缩 | 桌面 (D3D11+/Vulkan) |

### 5.2 实现计划

- [x] 定义 `dong_image_decoder.h` 接口
- [x] SDL backend: PNG/JPEG 解码 (stb_image)
- [x] SDL backend: ASTC 4x4/5x5/6x6/8x8 编码
- [x] SDL backend: BC7 编码
- [x] SDL backend: BC6H 编码 (HDR)
- [x] 定义 `dong_image_atlas.h` 接口（支持 block-aligned 分配）
- [x] SDL backend: ImageAtlas 实现
- [x] 渲染器集成 ImageAtlas（替换旧的手动管理）
- [x] 压缩格式 Atlas 支持（通过环境变量 DONG_ATLAS_FORMAT 启用）
- [x] 图像压缩编码集成（ensureImageInAtlas 自动编码）
- [x] 渲染器压缩纹理采样（GPU 硬件自动解压，shader 无需修改）
- [x] 单元测试

---

## 六、View/Engine 双维护问题

| 路径 | 文件 | 说明 |
|------|------|------|
| 旧 View | `src/core/view.cpp` (1800+ 行) | SDL 强耦合 |
| 新 Engine | `src/core/engine_view.cpp` | 较干净 |

**问题**: DOM/Layout/Script/渲染逻辑存在两套，bugfix 需要双改

**方案**: 以 Engine 为核心，View 变为薄包装

---

## 七、完成记录

| 日期 | 完成项 | 提交 |
|------|--------|------|
| 2026-02-03 | 定义 `dong_image_decoder.h` 接口 | - |
| 2026-02-03 | 实现 SDL ImageDecoder (stb_image 解码) | - |
| 2026-02-03 | 更新 `dong_platform.h` 添加 ImageDecoder 注入 | - |
| 2026-02-03 | 创建 `tests/test_image_decoder.c` 单测 | - |
| 2026-02-03 | 实现 ASTC 编码器 (`dong_astc_encoder.c`) | - |
| 2026-02-03 | 实现 BC7/BC6H 编码器 (`dong_bc_encoder.c`) | - |
| 2026-02-03 | 集成编码器到 SDL ImageDecoder | - |
| 2026-02-03 | 重构 `resource_manager.cpp` 移除 SDL 依赖 | - |
| 2026-02-03 | 定义 `dong_image_atlas.h` 接口（block-aligned 分配） | - |
| 2026-02-03 | 实现 SDL ImageAtlas (`dong_sdl_image_atlas.c`) | - |
| 2026-02-03 | 添加 ImageAtlas 单元测试 | - |
| 2026-02-03 | 渲染器集成 ImageAtlas（替换旧的 image_atlas_texture_） | - |
| 2026-02-03 | 添加压缩格式 Atlas 支持（GPU 检测 + 自动编码） | - |
| 2026-02-03 | glyph_atlas.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | gpu_device.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | gpu_painter.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | gpu_surface.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | shader_manager.cpp 移除 SDL_Log，使用 DongLogger | - |
| 2026-02-03 | view.cpp 移除 SDL_Log (47处)，使用 DongLogger | - |

