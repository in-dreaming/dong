
重要：
- 核心思路：梳理重构整个架构，不留技术债。
- 原则：公共模块留在 core，通过能力注入隔离平台差异；backend 只做平台绑定与执行链。
- 禁止：为了“去 SDL”而把公共模块整体下沉到 backend（会导致每 backend 复制一份）。

# Dong 引擎重构遗留清单（持续更新）

> 最后更新: 2026-02-04
> 用途: 跟踪重构进度 / 纠正过时记录 / 明确下一步任务

---

## 一、SDL 依赖泄漏（Core 层不应有 SDL）

### 1.1 严重 - 必须重构

| 文件 | 问题 | 状态 | 方案1修正方向 |
|------|------|------|--------------|
| `src/core/view.cpp` | `#include <SDL3/...>`、SDL 类型遍布 | ❌ 未开始 | legacy/旧 view 路径：隔离或逐步退役；新架构走 plugin + engine_view |
| `src/core/view.hpp` | 前向声明 `SDL_*` 类型 | ❌ 未开始 | legacy 路径隔离；新路径不得出现 SDL 类型 |
| `src/render/glyph_atlas.hpp` | 直接包含 `SDL3/SDL_gpu.h`、暴露 `SDL_GPUTexture*` | ❌ 需修正 | **GlyphAtlas 回归 core**，通过注入的 GPUTexture/Atlas 能力管理纹理页（opaque handle） |
| `src/render/gpu_device.*` | SDL_GPU 依赖 | ⚠️ 需重新决策 | 方案1：GPUDevice 属于 backend；core 只通过注入能力访问资源/执行 |
| `src/render/shader_manager.*` | SDL_shadercross 依赖 | ✅ 迁移到 backend 后保留 | 方案1：ShaderManager 属于 backend |
| `src/render/gpu_surface.*` | SDL_GPU 依赖 | ⚠️ 需重新决策 | 方案1：SurfaceFactory 注入；Surface/GPU 具体实现由 backend 提供 |
| `src/render/gpu_painter.*` | SDL_GPU 依赖（或历史残留） | ⚠️ 需重新决策 | 方案1：Painter/Compiler 在 core；执行器在 backend |

### 1.2 架构问题 - 文件位置错误

| 文件 | 问题 | 状态 | 说明 |
|------|------|------|------|
| `src/platform/sdl3_window.*` | SDL 实现放在 core src 下 | ✅ 已迁移 | 已迁至 `backends/sdl/sdl_window.*` |
| `src/input/input_adapter_sdl3.*` | SDL 实现放在 core src 下 | ✅ 已迁移 | 已迁至 `backends/sdl/sdl_input_adapter.*` |
| `backends/sdl/sdl_glyph_atlas.*` | **公共模块被下沉**（错误方向） | ❌ 必须回滚/删除 | GlyphAtlas 应留在 core，通过能力注入访问纹理页 |
| `backends/sdl/dong_sdl_execute.*` | SDL 执行链双轨（仍引用旧 core SDL 渲染类） | ❌ 必须退役 | 方案1：SDL backend 唯一执行链为 `sdl_gpu_driver*` |

### 1.3 可接受 - Legacy 或已隔离

| 文件 | 说明 |
|------|------|
| `src/api_bindings_legacy.cpp` | Legacy API，SDL 耦合是预期的（需明确与新架构隔离） |

---

## 二、方案1：边界与能力注入（摘要）

> 详见新增文档：`doc/架构重构方案1_总体框架.md`

### 2.1 核心边界
- core：DOM/CSS/Layout/Script + DisplayList + GPU IR + ResourceManager + **GlyphAtlas（公共）**
- backend：窗口/输入/GPUDevice/Shader 管理 + GPUCommandList 执行 + 平台能力

### 2.2 必需的注入能力（平台无关）
- 已有：`DongFileSystem`、`DongLogger`、`DongImageDecoder`、`DongGPUDriver`、`DongSurfaceFactory`
- 方案1新增/强化：
  - **GPUTexture/GPUResource 能力语义补齐**：upload 支持 stride/subrect + 可选 fence/ticket
  - **AtlasFactory 能力**：core 通过工厂创建 `DongImageAtlas`，避免直接调用 `dong_sdl_image_atlas_create`

### 2.3 TODO：GPU 压缩（片元/compute）
- 当前：RGBA8 → `DongImageDecoder.encode()`（CPU）→ `DongImageAtlas.upload()`
- TODO：RGBA8 → GPU shader/compute → 直接写入压缩 atlas/texture（避免 CPU blocks 中转）

---

## 三、backends/sdl 归属审查（方案1）

| 模块 | 现状 | 归属结论 |
|------|------|----------|
| `sdl_window.*` / `sdl_input_adapter.*` | SDL 平台绑定 | ✅ backend 正确 |
| `sdl_gpu_driver*` / `sdl_shader_manager.*` / `shaders/*` | SDL GPU 执行链 | ✅ backend 正确（作为唯一执行链） |
| `dong_sdl_plugin.*` / `dong_sdl_platform.*` | plugin + 平台注入 glue | ✅ backend 正确 |
| `dong_sdl_image_atlas.*` | `DongImageAtlas` SDL 实现 | ✅ backend 正确（实现层） |
| `dong_sdl_image_decoder.*` | stb 解码 + CPU 编码能力（当前挂 SDL 名称） | ⚠️ 名称/归属需梳理：可保留为 backend 实现，但应避免把“纯 CPU 编码器”锁死在 SDL |
| `dong_astc_encoder.*` / `dong_bc_encoder.*` | 纯 CPU 编码器实现 | ⚠️ 应迁移为公共模块（避免多 backend 复制） |
| `sdl_glyph_atlas.*` | GlyphAtlas SDL 版 | ❌ 删除：公共模块应在 core |
| `dong_sdl_execute.*` | 旧 SDL 执行器（引用旧渲染类） | ❌ 退役：避免双轨 |

---

## 四、阶段记录（清理过时内容后重建）

### 4.1 阶段1：Backend Plugin 化（P0）
- [x] 新增 `backends/sdl/dong_sdl_plugin.*`
- [x] `dong_platform_set_*` 注入路径可用

### 4.2 阶段2：SDL GPU 执行链迁移（P0）
现状说明：
- `backends/sdl/sdl_gpu_driver*` 已存在并承载主要执行逻辑
- 但架构上仍存在双轨（`dong_sdl_execute.*`）需要退役

待办：
- [ ] 退役 `dong_sdl_execute.*`，保证 SDL backend 唯一执行入口
- [ ] `dong_sdl_platform.c` 的 `DongGPUDriverVTable.execute/prepare_resources/...` 统一调度到 `sdl_gpu_driver*`

### 4.3 阶段3：GlyphAtlas 回归 core（P0）⭐当前关键修正
- [ ] 删除 `backends/sdl/sdl_glyph_atlas.*`
- [ ] core 的 `GlyphAtlas` 去 SDL 化（不暴露 `SDL_GPUTexture*`）
- [ ] 定义并注入 `GlyphAtlasTextureOps`（或通用 `GPUTextureOps` 子集）
- [ ] SDL backend 提供该 ops 的实现

### 4.4 阶段4：GPUTexture / GPUAtlas 能力补齐（P1）
- [ ] `DongGPUDriver` 上传语义补齐：stride/subrect + fence/ticket（供 glyph/image/batch 统一使用）
- [ ] 增加 AtlasFactory（注入创建 `DongImageAtlas` 的能力）

### 4.5 阶段5：压缩纹理路线（P2，含 GPU 片元/compute TODO）
- [x] CPU 压缩：ASTC/BC 编码器可用（目前在 SDL backend 目录）
- [ ] 将 `dong_astc_encoder.*` / `dong_bc_encoder.*` 迁移为公共模块（避免 backend 复制）
- [ ] TODO：GPU 压缩（shader/compute）接口设计与实现排期

---

## 五、验收与回归

### 5.1 架构验收
- [ ] core 侧（非 legacy）无 SDL 头文件依赖
- [ ] backend 承载唯一 SDL 执行链（无双轨）
- [ ] 公共模块（GlyphAtlas/CPU 编码器等）不因 SDL 耦合而下沉

### 5.2 多帧一致性/闪烁排查工具链（保留）
- `examples/html_render_test.cpp` → `html_render_test.exe`
- `scripts/tools/html_baseline_render.py`
- `scripts/tools/vl_tool_multi.py`
- `scripts/tools/run_multiframe_regress.py`
- `scripts/tools/run_baseline_compare.py`

---

## 六、完成记录（保留真实发生，不再把“方向错误”标为完成）

| 日期 | 事项 | 备注 |
|------|------|------|
| 2026-02-03 | 定义 `dong_image_decoder.h` / `dong_image_atlas.h` 接口 | 已落地 |
| 2026-02-03 | SDL backend: stb 解码 + ASTC/BC CPU 编码集成 | 后续需把 CPU 编码器公共化 |
| 2026-02-04 | SDL backend: `sdl_window`/`sdl_input_adapter` 迁移 | 正确方向 |
| 2026-02-04 | SDL backend: `sdl_gpu_driver*` 迁移/拆分 | 作为唯一执行链保留 |
| 2026-02-04 | 发现并纠正：`GlyphAtlas` 不应下沉 backend | 方案1关键修正点 |

---
