

ES2020 ~ ES2022

class / async / await

module（但可限制）

html5

# bug
setLayerCacheEnabled 开关不一样，渲染结果会不一样。

applyDOMStylesToYoga 未实现的各种标准

Flexbox



# 标签
<script>
a) 仅支持 inline <script> 标签（推荐先做）
b) 同时支持 <script src="..."> 外部脚本
c) 包含 defer/async 属性支持


# engine

目前demo开发到一定阶段了。在继续完善功能前，需要重构当前仓库。 我希望从一下几个大方向展开重构
- 1.整体项目管理：examples去冗余，仅保留必要example。需要区分单测和example。
  - 必须保留3d_*、gpu_screenshot_demo、interactive_demo、其他单测视情况做裁剪。
- 2.dong引擎架构优化
  - 渲染管线：是否合理，是否扩展性强，是否可优化，模块是否低耦合高内聚等，rt等资源管理、使用是否合理
  - web内核：html、css、事件系统等模块设计是否合理，是否可优化，模块是否低耦合高内聚等。这块要尤其注意，未来会支持更多规范，比如html5、css3、event等。如果架构不合理将导致扩展性大打折扣。
  - 其他模块：比如字体、图片、音频等模块设计是否合理，是否可优化，模块是否低耦合高内聚等。


我希望优化思路是：
- 1.优化代码结构
- 2.优化代码质量：模块划分，代码规范，代码复用、低耦合、高内聚等。
- 3.优化代码性能

重构完成后，需要跑通所有保留的单测、demo。

优化架构
- 各个模块拆分是否合理
- 渲染管线性能、内存等是否合理