---
description: 优化性能的一些思路
globs:
alwaysApply: false
---

- 先用 **trace/采样** 把大头精确定位到函数/子阶段；没有数据就不要动手优化。
- 优先处理 **确定性的重复工作/多余遍历/无效排序/不必要的 rebuild**，这类改动往往是“量级级”收益。
- 只在热点确认后再做“细节优化”（string/小分配/微循环）；否则容易陷入噪声。
- 每次改动都要 **复测同一场景**，关注 count/sum/max 的变化，并记录对比结论。
